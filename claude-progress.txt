Contribution Arc — スマホ版 自動化ループ 進捗ログ
==================================================

## このセッション（足場のみ・機能実装なし）
- リポジトリ全体を把握（React+Vite+Firebase / Capacitor(SPM) iOS, 状態駆動の単一SPA）。
- acceptance-criteria.md を作成（3ゴール → AUTO / HUMAN に分解）。
- verify.sh を作成（WEB 層 / NATIVE 層、状態は PASS/FAIL/SKIP）。
- テスト雛形を配置: Playwright e2e, Firestore rules test, Maestro flow, Lighthouse budget。
- 禁止事項は遵守: archive / 署名 / App Store 提出 / cap submit 系は未着手。

## verify.sh 初回実行結果（mode=all, RUN_NATIVE=0）
日時: 2026-06-21
集計: PASS=6  FAIL=0  SKIP=16

### 緑（PASS=6）— 既存コードのベースラインは健全
- TYPECHECK          tsc --noEmit
- BUILD              vite build
- F-LISTENER-LEAK    解除されない onSnapshot は無し
- F-LISTENER-BUDGET  onSnapshot 呼び出し 12 / 上限 14
- N-TOOL-XCODE       xcodebuild あり
- N-TOOL-SIMCTL      xcrun(simctl) あり

### ハード FAIL（赤）=0
今回、実際に走って失敗したチェックは無い。
※「赤」の実体は下記 SKIP（ツール未導入・未配線で未だ緑にできていない項目）。

### SKIP=16（＝これから緑にしていく対象）
ツール未導入:
- LINT               eslint 未設定
- UNIT               vitest 未導入
- N-TOOL-MAESTRO     maestro 未導入
- W-LH-PERF-BUDGET   @lhci/cli 未導入

未配線（雛形はあるが中身/依存が未整備）:
- W-E2E-HOME-POST      playwright 未導入
- W-E2E-HOME-SCROLL    playwright 未導入
- W-SCROLL-LONGTASK    playwright 未導入
- W-E2E-RECORD-CRUD    playwright 未導入
- W-E2E-RECORD-PERSIST playwright 未導入
- F-RUNTIME-BUDGET     playwright + Firestore計測 未配線
- RULES-RECORD-OWNER   firestore rules test 未配線（vitest/emulator）
- RULES-DENY-DEFAULT   firestore rules test 未配線（vitest/emulator）

ネイティブ重ステップ（RUN_NATIVE=1 で実行・今回は未実行）:
- N-CAP-SYNC, N-IOS-BUILD, N-MAESTRO-SCROLL, N-MAESTRO-KEYBOARD

## 次の周で最初に潰す1項目（提案）
**Playwright e2e の配線（@playwright/test 導入 + テスト用ログインの導線）。**
理由: 1つ導入するだけで W-E2E-HOME-POST / HOME-SCROLL / RECORD-CRUD /
RECORD-PERSIST / SCROLL-LONGTASK / F-RUNTIME-BUDGET の最大6項目が動き出し、
ゴール①②の中核（自由投稿・記録CRUD・スクロール品質）を一気にカバーできる。
longtask 計測と Firestore 読み書き計測も同じ Playwright run に相乗りできる。
※新ライブラリ導入になるため、着手前にユーザー確認が必要。

補欠: LINT（eslint 設定）は最も低コストで緑にでき、③のコード品質シグナルを得られる。

## 品質緑化ループ（高品質方針・Emulator Suite 土台）
方針: 1イテレーション=1チェック群を本物のテストで緑化→コミット。push は節目集約。

### R1 — ESLint 基盤（LINT 緑）2026-06-21
- eslint.config.js（flat config: @eslint/js + typescript-eslint + react-hooks +
  react-refresh）。dist/build/ios/electron/functions/lib 等を ignore。
- rules-of-hooks=error。React Compiler 系の助言ルール（set-state-in-effect 等）は
  実リファクタが要るため warn に降格（スコープ外）。実エラー8件は原因箇所で修正。
- 結果: 0 errors / 79 warnings（exit 0）。LINT PASS。

### R2 — Vitest + Emulator seam（UNIT 緑・テスト土台）2026-06-21
- src/firebase.ts に env ガード seam を追加（VITE_USE_EMULATORS=true 時のみ
  Auth/Firestore Emulator に接続。本番動作は不変）。
- firebase.json に emulators ブロック（auth:9099, firestore:8080）。
- vitest.config.ts + tests/unit/format.test.ts（純関数9本）。UNIT PASS。

### R3 — Firestore rules test（RULES 緑）2026-06-21
- tests/firestore/rules.test.ts を実装（12 tests）。実ルール準拠:
  learningItems/posts は authed read・owner write、studyLogs は owner-only、
  studyMinutes>1440 拒否、未定義パスは default deny。
- verify.sh の RULES 分岐を firebase emulators:exec 経由に更新（emulator 必須）。
- package.json に test:rules スクリプト追加。RULES PASS。

### R4 — Playwright e2e 基盤+認証+CRUD（W-E2E 緑）2026-06-21
- @playwright/test 導入 + chromium。playwright.config は専用ポート(5279)で
  自前起動（reuse 禁止：過去に別アプリ Madoromi の dev サーバを誤掴み）。
  mobile-chromium（Pixel7 viewport + longtask 対応）。
- global-setup: Auth Emulator REST で test user を seed → uid を取得 →
  Firestore Emulator REST(Bearer owner)で users/{uid} に
  onboardingCompletedAt+userId を seed（初回オンボーディングを skip）。
- src に data-testid 追加（表示・スタイル非干渉）: ログインフォーム、ホーム
  composer、bottom-nav(home/learning)、ライブラリ追加/編集/削除、feed-post。
- tests/e2e: home-feed.spec（投稿出現・スクロール欠落なし）、
  study-records.spec（学習対象 作成→編集→削除、リロード永続）。
- verify.sh の W-E2E 分岐を firebase emulators:exec --only auth,firestore で
  ラップ。package.json に test:e2e。
- longtask / F-RUNTIME-BUDGET は test.fixme のまま（→R5）。

### R5 — スクロール longtask + Firestore runtime budget 確定 2026-06-21
- W-SCROLL-LONGTASK: home-feed.spec に実装。投稿14件を用意し
  PerformanceObserver('longtask') を仕込んでプログラム的に上下スクロール、
  50ms 超タスクを数える（上限5）。実測 total=0 over50=0（headless で滑らか）。
  カクつき回帰の機械的ガードとして機能。
- F-RUNTIME-BUDGET: 生の reads/writes ではなく WebChannel の POST バースト数を
  page.on("request") で計測（読み取り=/Listen/channel, 書き込み=/Write/channel）。
  production code には一切触れない非侵襲計測。代表シナリオ（ログイン→学習対象1件
  作成→投稿1件）で実測 listen=6 / write=8。flaky 回避のヘッドルームを乗せ
  LISTEN_BUDGET=30 / WRITE_BUDGET=20 に確定。acceptance-criteria.md の予算定義も
  WebChannel バースト基準に更新（暫定の reads≤300/writes≤30 を置換）。
- e2e は計6本全緑。verify.sh web は PASS=8 FAIL=0 SKIP=1（残 W-LH のみ）。

### R6 — Lighthouse CI（W-LH-PERF-BUDGET 緑・Web 層完走）2026-06-21
- @lhci/cli 導入。lighthouserc.json を staticDistDir 配信から vite preview 配信に
  変更（dist を本番同様 /Contribution-Arc/ サブパスでサーブ。ルート配信だと
  base 付きアセットが 404 になるため）。startServerCommand で計測。
- 実測（desktop preset）: perf=0.98 / TBT=0ms / CLS=0 / LCP=821ms / TTI=822ms /
  script=513KB / total=634KB。
- preset 選定: モバイル throttling だと perf=0.70/LCP=5.4s だが、native は file://
  読み込みでネット律速が代表性を欠くため desktop を決定論的回帰ガードに採用。
- 予算: タイミング系はヘッドルーム維持（TBT≤300/CLS≤0.1/LCP≤3000/TTI≤4000）、
  バンドルサイズ系は実測直上に締める（script≤750KB/total≤1100KB）。
- package.json に test:lh（vite build && lhci autorun）、.gitignore に .lighthouseci。

### 現在の verify.sh web 集計: PASS=9 FAIL=0 SKIP=0（Web 層 全緑）
W-SCROLL-LONGTASK / F-RUNTIME-BUDGET は R5 で有効化済み（W-E2E に相乗り）。
native（N-*）は RUN_NATIVE=1 時のみ（→R7）。
→ 節目: ios-auth を main にマージして push（deploy 反映）。

## メモ
- iOS は Capacitor 8 = SPM 構成（Podfile/.xcworkspace なし）。scheme は "App"、
  verify.sh は ios/App/App.xcodeproj を直接ビルドする。
- Firestore 予算しきい値（MAX_LISTENERS=14, reads≤300, writes≤30）は暫定。
  F-RUNTIME-BUDGET を実測して確定すること。
